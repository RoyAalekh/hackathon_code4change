services:
  - type: web
    name: court-scheduler
    env: docker
    plan: starter
    autoDeploy: true
    dockerCommand: >
      uv run scheduler/dashboard/app.py
      --server.port 8501
      --server.address 0.0.0.0
    envVars:
      PORT: 8501
